<Window x:Class="drawing_app.ImportBrushWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="Import New Brush" Height="450" Width="800"
        WindowStartupLocation="CenterScreen">
    <StackPanel Margin="12" VerticalAlignment="Top">

        <TextBlock Text="Brush Name" />
        <TextBox x:Name="NameBox" Margin="0,4,0,8"/>

        <TextBlock Text="Thickness" />
        <Slider x:Name="SizeSlider" Minimum="1" Maximum="100" Value="10"/>
        
        <TextBlock Text="Opacity" Margin="0,8,0,0"/>
        <Slider x:Name="OpacitySlider" Minimum="1" Maximum="255" Value="255"/>

        <TextBlock Text="Spacing" Margin="0,8,0,0"/>
        <Slider x:Name="SpacingSlider" Minimum="0.05" Maximum="1" Value="0.25"/>

        <Button Content="Choose Brush Image"
                Margin="0,12,0,0"
                Click="OnChooseImage"/>

        <Image x:Name="PreviewImage"
               Height="100"
               Margin="0,8,0,0"
               Stretch="Uniform"/>

        <StackPanel Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,16,0,0">
            <Button Content="Cancel"
                    Width="80"
                    Margin="0,0,8,0"
                    Click="OnCancel"/>
            <Button Content="Save"
                    Width="80"
                    Click="OnSave"/>
        </StackPanel>

    </StackPanel>
</Window>
