<Window x:Class="drawing_app.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
        xmlns:local="clr-namespace:drawing_app"
        mc:Ignorable="d"
        Title="SimpleART">
    <Grid>
        
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <Menu Grid.Row="0"
              Background="#F3F3F3"
              Padding="8"
              FontSize="14">

            <MenuItem Header="File">
                <MenuItem Header="Save" Click="Save_Click" />
                <MenuItem Header="Save As..." Click="SaveAs_Click" />
                <MenuItem Header="Exit" Click="Exit_Click"/>
            </MenuItem>

            <MenuItem Header="Import">
                <MenuItem Header="Import New Brush"
                          Click="ImportBrush_Click"/>
            </MenuItem>
            
            <MenuItem Header="Undo"
                      Command="{Binding UndoCommand}"
                      InputGestureText="Ctrl+Z"/>
         
            
            <MenuItem Header="Redo"
                      Command="{Binding RedoCommand}"
                      InputGestureText="Ctrl+Y"/>
          

        </Menu>

        <Grid Grid.Row="1">
            <ScrollViewer HorizontalScrollBarVisibility="Auto"
                  VerticalScrollBarVisibility="Auto"
                  Background="#E5E5E5"
                  HorizontalContentAlignment="Center"
                  VerticalContentAlignment="Center"
                  HorizontalAlignment="Stretch"
                  VerticalAlignment="Stretch"
                  CanContentScroll="False">
                

                <Border BorderBrush="#DDD"
                        BorderThickness="1"
                        Background="White"
                        Margin="340,20, 240, 20"
                >

                    <local:CanvasControl x:Name="DrawingCanvas"
                                         Width="1100"
                                         Height="800"
                                         HorizontalAlignment="Left"
                                         VerticalAlignment="Top">

                        <local:CanvasControl.LayoutTransform>
                            <ScaleTransform x:Name="CanvasScaleTransform"
                                            ScaleX="1"
                                            ScaleY="1"/>
                        </local:CanvasControl.LayoutTransform>

                    </local:CanvasControl>
            </Border>
            </ScrollViewer>


            <Border Width="320"
                Background="#FAFAFA"
                HorizontalAlignment="Left"
                BorderBrush="#CCC"
                BorderThickness="0,0,1,0">
            <StackPanel>

                <Grid>
                    <xctk:ColorCanvas x:Name="ColorPicker"
                                      Height="240"
                                      SelectedColorChanged="ColorPicker_SelectedColorChanged"/>

                   
                    <Button 
                            Width="32"
                            Height="32"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Top"
                            Margin="5"
                            Click="PickColor_Click">
                        <Image Source="icons/eyedropper.png" Width="20"/>
                    </Button>
                </Grid>

                <TextBlock Text="Brushes" Margin="5,10,5,5"/>

                <ListBox x:Name="BrushList"
                         Height="150"
                         DisplayMemberPath="Name"
                         SelectionChanged="BrushList_SelectionChanged">

                    <ListBox.ContextMenu>
                        <ContextMenu>
                            <MenuItem Header="Delete Brush"
                                      Click="DeleteBrush_Click"/>
                        </ContextMenu>
                    </ListBox.ContextMenu>
                </ListBox>

                <TextBlock Text="Thickness:" Margin="5,0"/>
                <Slider x:Name="ThicknessSlider"
                        Minimum="1"
                        Maximum="150"
                        Value="4"
                        Margin="5"
                        ValueChanged="ThicknessSlider_ValueChanged"/>

                <TextBlock Text="Opacity:" Margin="5,0"/>
                <Slider x:Name="OpacitySlider"
                        Minimum="0"
                        Maximum="255"
                        Value="255"
                        Margin="5"
                        ValueChanged="OpacitySlider_ValueChanged"/>

            </StackPanel>
        </Border>


        <Border Width="220"
        Background="#F3F3F3"
        HorizontalAlignment="Right"
        BorderBrush="#CCC"
        BorderThickness="1,0,0,0"
        Padding="10">

    <Grid>
        
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>     
            <RowDefinition Height="Auto"/>  
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">

            <TextBlock Text="Layers" FontWeight="Bold" Margin="0,0,0,10"/>

            <ListBox x:Name="LayerList"
                     DisplayMemberPath="Name"
                     SelectionChanged="LayerList_SelectionChanged"
                     Height="300"/>

            <TextBlock Text="Layer Opacity:" Margin="0,10,0,0"/>

            <Slider x:Name="LayerOpacitySlider"
                    Minimum="0"
                    Maximum="1"
                    Value="1"
                    TickFrequency="0.01"
                    Margin="0,0,0,10"
                    ValueChanged="LayerOpacitySlider_ValueChanged"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Width="40" Height="40" Margin="5" Click="AddLayer_Click">
                    <Image Source="/icons/plus.png" Width="20"/>
                </Button>
                <Button Width="40" Height="40" Margin="5" Click="DeleteLayer_Click">
                    <Image Source="/icons/trash.png" Width="20"/>
                </Button>
            </StackPanel>

        </StackPanel>

     
        <StackPanel Grid.Row="1"
                    Margin="0,20,0,0"
                    VerticalAlignment="Bottom">

                <Separator Margin="0,10"/>

                <TextBlock Text="Zoom"
                           HorizontalAlignment="Center"
                           Margin="0,5"/>

                <StackPanel Orientation="Horizontal"
                            HorizontalAlignment="Center">

                    <Button Width="25" 
                            Height="25"
                            Click="ZoomOut_Click">
                        <Image Source="/icons/zoomout.png" Width="20"/>
                    </Button>

                    <Slider x:Name="ZoomSlider"
                            Minimum="0.25"
                            Maximum="4"
                            Value="1"
                            Width="120"
                            Margin="5,0"
                            ValueChanged="ZoomSlider_ValueChanged"/>

                    <Button Width="25" 
                            Height="25"
                            Click="ZoomIn_Click">
                        <Image Source="/icons/zoomin.png" Width="20"/>
                    </Button>

                </StackPanel>

        </StackPanel>

        </Grid>

        </Border>

        </Grid>
       
   
    </Grid>
</Window>
